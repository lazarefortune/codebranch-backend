services:
  db:
    image: mysql:8.0
    restart: always
    env_file:
      - .env
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-codebranch}
    volumes:
      - mysqldata:/var/lib/mysql

volumes:
  mysqldata: